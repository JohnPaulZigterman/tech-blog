<div class="comments">
    {{#each this}}
    <div class="card">
        <h3 class="card-header">{{content}}</h3>
        <p class="card-body">-{{user.name}}, {{format_date created_at}}</p>
    </div>
</div>