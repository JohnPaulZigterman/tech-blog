{{#if posts.length}}
<div>
    <h2>Your Posts:</h2>
    {{#each posts as |post|}}
    {{> dash-info post}}
    {{/each}}
</div>
{{/if}}

<button type="submit">
    <a href="/dashboard/new">+ New Post</a>
</button>

<script src="/public/js/add-post.js"></script>