{{#if posts.length}}
<div>
    <h2>Your Posts:</h2>
    {{#each posts as |post|}}
    {{> dash-info post}}
    {{/each}}
</div>
{{/if}}

<button type="submit" id="new-post-button">
    <a href="/dashboard/new">+ New Post</a>
</button>

<script src="/js/add-post.js"></script>